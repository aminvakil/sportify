{% extends 'base.html.twig' %}

{% block body %}
<div class="container content login-screen">
    <div class="row">
        <div class="col-sm-6 col-sm-offset-3 text-center">
            <h2>Registration confirmed</h2>
            {% if targetUrl %}
                <p>
                    <a href="{{ targetUrl }}">Back</a>
                </p>
            {% endif %}
        </div>
    </div>
</div>
{% endblock %}
